<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <title><%= data.goodsName %></title>
    <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65"
            crossorigin="anonymous"/>
    <link rel="stylesheet" href="/css/rest.css"/>
</head>
<script>
    window.addEventListener('load', function () {
        const quantity = document.querySelector('#quantity');
        const price = document.querySelector('#price');
        const total = document.querySelector('#totalPrice');

        function updateTotal() {
            const quantityValue = parseInt(quantity.value, 10);
            const priceValue = parseInt(price.textContent, 10);
            total.textContent = priceValue * quantityValue;
        }

        quantity.addEventListener('input', updateTotal);
        updateTotal();
    });

</script>

<body>
<%- include('includes/head.ejs') %>
<script src="/javascripts/addCart.js"></script>
<main>
    <div class="detailGroup">
        <div class="rowGroup">
            <% let imgName = ['chickenbokk', 'mara', 'shabushabu', 'steak']; %>
            <% let randomNumber = Math.floor(Math.random() * 4); %>
            <img src="/images/<%= imgName[randomNumber] %>.jpg" alt="" style="width: 30%; margin: auto"/>
            <div class="orderBlock">
                <span>상품 이름 : <%= data.goodsName %></span>
                <span>상품 가격 : <span id="price"><%= data.price %></span></span>
                <span>수량 :  <input type="number" id="quantity" min="1" value="1"></span>

                <span>Total : <span id="totalPrice"></span> </span>
                <button type="button" class="darkButton" style="width: 150px;" onclick="addCart()">Add to Cart</button>
            </div>
        </div>


        <div class="detailGroup">
            <span>상세 설명 : <br>
                <%= data.detail %></span>
        </div>

    </div>
</main>
</body>
</html>
